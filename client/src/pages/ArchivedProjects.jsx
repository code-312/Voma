import React, { useState, useEffect }  from 'react';

const ArchivedProjects = () => {
    const [projects, setProjects] = useState([]);

    return <h1 style={{ paddingTop: '100px' }}>Archive!</h1>
};

export default ArchivedProjects;
