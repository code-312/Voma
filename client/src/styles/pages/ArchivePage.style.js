import styled from 'styled-components';

export const ArchiveHeader = styled.div`
  label {
    display: block;
  }
`;

export const ArchiveFooter = styled.div`
  border-top: 1px solid var(--peachShade1);
  margin: 16px -24px 0px -24px;
  padding: 8px 0 0 8px;
  button {
    display: inline-block;
    margin: 8px;
  }
`;
